import { defineMessages } from 'react-intl';

export const scope = 'caregiver.containers.UserSettingsPage';

export default defineMessages({
  userSettingsHeader: {
    id: `${scope}.user_settings.header`,
    defaultMessage: 'User Settings',
  },
});
